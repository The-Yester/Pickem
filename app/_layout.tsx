// In app/_layout.js (or app/_layout.tsx)

import RootAppNavigator from '../App'; // Assuming your App.js is in the parent (root) directory

export default function RootLayout() {
  return <RootAppNavigator />;
}
